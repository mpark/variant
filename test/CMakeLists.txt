cmake_minimum_required(VERSION 2.8)

add_definitions(-std=c++14 -Wall -Wextra -Werror)

function(add_test_case name)
  add_executable(${name}.test ${name}.cpp)
  target_link_libraries(${name}.test gtest gtest_main pthread)
  add_test(${name} ${name}.test --gtest_color=yes)
endfunction(add_test_case)

# add_test_case(assign.ref)
# add_test_case(assign.val)
add_test_case(cnstr.conversion)
add_test_case(cnstr.default)
add_test_case(cnstr.in_place)
# add_test_case(cnstr.ref)
# add_test_case(cnstr.val)
# add_test_case(dtor)
# add_test_case(get)
# add_test_case(hash)
add_test_case(intro)
# add_test_case(swap)
# add_test_case(type.switch)

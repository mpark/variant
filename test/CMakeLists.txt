cmake_minimum_required(VERSION 2.8)

add_definitions(-std=c++14 -Wall -Wextra -Werror)

function(add_test_case name)
  add_executable(${name} ${name}.cpp)
  target_link_libraries(${name} gtest gtest_main pthread)
  add_test(${name} ${name} --gtest_color=yes)
endfunction(add_test_case)

add_test_case(access.get)
add_test_case(access.get_if)
add_test_case(access.holds_alternative)
add_test_case(assign.copy)
add_test_case(assign.conversion)
add_test_case(assign.emplace)
add_test_case(assign.move)
add_test_case(cnstr.copy)
add_test_case(cnstr.conversion)
add_test_case(cnstr.default)
add_test_case(cnstr.in_place)
add_test_case(cnstr.move)
add_test_case(dtor)
add_test_case(hash)
add_test_case(intro)
add_test_case(rel)
add_test_case(swap)
add_test_case(visit)
